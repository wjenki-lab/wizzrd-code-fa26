console.log("hello world")[]

function setup(){
    createCanvas(windowWidth, 
        windowHeight)
}

function draw() {
    //background(400)
    rectMode(CENTER)
    rect(mouseX, mouseY, 50, 50)
function windowResized() {}    
}