console.log("hello world")[id]

function setup(){
    createCanvas(windowWidth, 
        windowHeight)
}

function draw(rect) {
    rectMode(CENTER)
        rect(mouseX, mouseY, 50, 50)}
function windowResized() {50}    